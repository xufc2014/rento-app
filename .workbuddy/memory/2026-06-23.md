# 2026-06-23 工作日志

## Android APK 导出/导入 bug 修复

三个 bug 都是打包成 Android APK 后出现的：

### Bug 1 — 导出 Excel 点击无反应
- **文件**: `utils/export.js`
- **根因 1**: `saveAsCSV_App` 用了 `plus.io.PUBLIC_DOCUMENTS`（不存在的常量），Android 上静默失败
- **根因 2**: `writer.write(content)` 先于 `writer.onwrite` 回调赋值，异步竞态导致回调永不触发
- **修复**: 改用 `plus.io.PUBLIC_DOWNLOADS`；先赋值回调再 write；失败时自动降级到 `plus.io.PRIVATE_DOC`；新增 `saveToPrivate()` 兜底函数；弹 Modal 展示完整文件路径

### Bug 2 — 导出配置提示保存成功但找不到文件
- **文件**: `pages/settings/index.vue`
- **根因**: Modal 只显示文件名不显示路径，用户不知道去哪找
- **修复**: 用 `plus.io.convertLocalFileSystemURL()` 获取真实路径并展示；写入成功后弹 "是否分享" 选择框 → 确认则调用 `plus.share.sendWithSystem({type:'file'})` 系统分享面板；写入失败自动降级到私有目录

### Bug 3 — 导入配置点击无反应
- **文件**: `pages/settings/index.vue`
- **根因**: `document.createElement('input')` + `input.click()` + 立即 `removeChild(input)` — Android WebView 中文件选择弹窗是异步的，用户选择文件时 input 节点已销毁，change 事件丢失
- **修复**: 
  - `removeChild` 移到 `onchange` 回调内部执行（选完文件后再移除）
  - `click()` 包裹在 `setTimeout(100ms)` 中确保 DOM 挂载完成
  - H5 版本同步修改保持一致

## 模拟器实测修复 — 导入仍然报错
- **现象**: 导出CSV和JSON正常，但导入报 `TypeError: Cannot read property 'createElement' of undefined`
- **根因**: uni-app APP模式下 `<script>` 在独立JS引擎(JSCore/V8)中执行，不在WebView渲染线程中，根本不存在 `document` 对象。之前 Bug 3 的修复方案 (`document.createElement`) 对 APP 端无效
- **正确方案**: APP端使用 `uni.chooseFile()` + `plus.io.resolveLocalFileSystemURL()` + `plus.io.FileReader()`
- H5 端保持 `document.createElement` 不变（H5运行时 JS 在浏览器主线程，document 可用）

## uni.chooseFile 不可用 → 三级降级方案
- HBuilderX 4.45 模拟器上 `uni.chooseFile` 不可用，需降级到原生 Android Intent
- **三级降级**: uni.chooseFile() → plus.android Intent (ACTION_OPEN_DOCUMENT) → 手动粘贴提示
- Intent 文件选择器初始用 `application/json` 作 MIME 类型，导致部分文件管理器过滤掉 data 目录
- **修复**: Intent MIME 类型改为 `*/*` 显示所有文件，同时保留 JSON 相关类型的 EXTRA_MIME_TYPES
- **语法错误修复**: showPasteImportFallback 下多余的 `}` 导致编译失败

## 合同签订-选租客优化
- **问题**: 步骤2「选租客」把数据库所有租客全量列出来，几百个租客时列表极长，而且新增租客时显示其他租客信息无意义
- **修复**: 改为搜索框 + 过滤结果模式
  - 新增 `tenantSearch` 搜索输入框，输入姓名/手机号实时过滤 `filteredTenants`
  - 不输入时显示提示文案「输入姓名搜索已有租客，或点击下方新增」
  - 输入无匹配时显示「未找到匹配的租客」
  - 选中后显示蓝色提示条「已选：xxx」
  - `onShow` 中自动检测新增租客：如果租客数增加且之前未选，自动选中最新一条

## 版本回退后重新实现 — 密码保护功能
- 回退到 `bbdc328 优化录入租客信息功能`，重新逐个实现功能
- **新增文件**: `utils/password.js` — 基于 Vue `ref()` 的可复用密码确认控制器，密码 159753
- **集成页面**:
  - `pages/settings/index.vue`: 清空所有数据、导入数据恢复、生成测试数据 三操作加密码验证
  - `pages/room/list.vue`: 删除楼栋、批量删除房间 两操作加密码验证
- **流程**: 原确认弹窗 → 用户确认 → 密码弹窗 → 输对密码 → 执行操作
- 已提交到 git 缓存区

## 重新实现 — 水电抄表管理功能（第2个功能）

### 1. db.js 新增方法
- **`getMonthReading(roomId, meterType, month)`**: 查询某房间某类型在指定月份是否已有抄表记录
- **`updateMeterReading(id, updates)`**: 修改抄表记录，自动重算 consumption/previousValue，并级联更新后续记录的 previousValue。修改后清除异常标记。

### 2. reading.vue 改造 — 已抄类型锁定
- 新增 `waterMonthReading`/`electricMonthReading`/`gasMonthReading` 响应式状态
- `loadLastReadings()` 同时检查当月已抄记录
- 模板: 每种表类型显示两种状态
  - 已录入 → 锁定卡片（显示读数 + "如需修改请到抄表管理"链接）
  - 未录入 → 正常输入框
- `canSubmit`: 水电至少一个已录入（历史或本次），且至少一个本次新输入
- `submitBtnText`: 全部已抄完 → "本月已全部抄完"；有未填 → 提示具体缺失类型
- `doSubmit`: 只提交当月未录入的类型（跳过已有记录）
- `refreshSavedStatus` 改用 `usageMonth` 字段匹配（与 getMonthReading 一致）
- 顶部新增「抄表管理」入口按钮
- 全部已抄时隐藏拍照和备注区域

### 3. 新建 pages/meter/manage.vue — 抄表管理页面
- 月份筛选（picker，默认上月）+ 楼栋筛选
- 按房间分组展示所有抄表记录
- 每条记录显示：类型标签、日期、读数、上次读数→用量、备注、异常标记
- **修改功能**: 点击 ✏️修改 → 弹窗编辑读数和备注 → 调用 `db.updateMeterReading()` → 自动重算用量并级联更新
- 修改后同步到抄表页面（共享同一 db 存储）

### 4. pages.json 新增路由 `pages/meter/manage`

## 优化 — 抄表操作日志显示房间信息

- **问题**: 操作日志中 `抄表录入` 和 `修改抄表` 只显示 `water: 0→125, 用量125`，不显示哪个房间
- **修复**: `db.js` 中 `addMeterReading()` 和 `updateMeterReading()` 的 `logOperation` detail 字符串加入楼栋+房间号信息
- **效果**: 日志变成 `A栋 3F-201 水表: 0→125, 用量125`，一目了然

## 优化 — 抄表支持逐项录入（水电可分开录）

- **问题**: `canSubmit` 要求 `hasWater && hasElectric`，管理员必须先录水电才能保存，不符合实际抄表流程（电表和水表位置分开，管理员先抄完所有电表再抄水表）
- **修复**:
  - `canSubmit`: 移除 `hasWater && hasElectric` 约束，只要有新输入数据就能提交
  - `submitBtnText`: 改为 `保存水表数据` / `保存电表数据` 等，告知用户具体保存了哪些类型
  - 新增 `partialRoomIds`：只录了水电其中一种的房间显示橙色「部分抄」徽章
  - `refreshSavedStatus`: 同时计算 fully-done 和 partially-done 两套集合
  - Template: 房间卡片新增 `.partial-badge` 橙色徽章样式

## 优化 — 合同初始读数全链路完善

- **问题1**: 签订合同时录了初始读数（底读），但合同详情页看不到
- **问题2**: 编辑合同没有初始读数的修改入口
- **问题3**: 抄表时上次读数显示"无记录"，实际合同里有初始读数
- **修复**:
  - `pages/contract/detail.vue`: 查看模式新增初始读数展示区（水/电/气底读）；编辑模式新增三个输入框可修改
  - `utils/db.js`: `updateContract` 的 allowedFields 加入三个初始读数字段；新增 `_syncInitialReadings()` 方法，修改初始读数时自动同步更新抄表记录（增/改/删）；新增 `getInitialReadingsForRoom()` 返回活跃合同的初始读数 + 日期
  - `pages/meter/reading.vue`: `loadLastReadings()` 中当 `getLatestReading` 返回 null 时，回退到合同初始读数作为上次读数
  - `doSubmit` 自动跳下一间：优先跳到未完全抄完的房间（含部分抄）
